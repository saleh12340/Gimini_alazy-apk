buildTypes {
    release {
        signingConfig signingConfigs.release
        minifyEnabled false      // طفيناه عشان R8 ما يضرب
        shrinkResources false    // طفيناه
        proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
    }
}
